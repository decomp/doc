% --- [ Code Generation Tool ] -------------------------------------------------

\subsection{Code Generation Tool}

foo

% * BasicBlock interface!
%    - A primitive may conceptually be represented as a basic block, thus simplifying the code vastly (not having to special case between two representations).
